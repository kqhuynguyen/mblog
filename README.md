A simple blog made in Nextjs, Tailwind and Tinacms.

# Getting started

1. Install npm modules by running `npm install` or `yarn install`.
2. Run `yarn-dev` to start the development server.
3. Visit localhost:3000.
